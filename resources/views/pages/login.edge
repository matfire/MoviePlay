@layout()
<div class="grid grid-cols-1 lg:grid-cols-2">
    <div class="w-full flex flex-col justify-center h-full px-4 items-center">
        <h1 class="capitalize">{{t('auth.login')}}</h1>
        <div>
            <a class="inline-flex items-center capitalize px-4 py-2 border border-black dark:border-white rounded-lg"
                href="{{route('login.github_redirect')}}">
                {{svg('mdi:github')}}
                {{t("auth.github_login")}}</a>
        </div>
    </div>
    <div class="hidden lg:block max-h-full">
        <img src="{{asset('resources/img/theatre-front.jpg')}}" class="" />
    </div>
</div>
@end